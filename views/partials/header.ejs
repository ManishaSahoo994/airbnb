<header class="bg-red-600 shadow-md py-2 px-8 flex items-center justify-between">
        <h1 class="text-2xl font-bold text-white">Airbnb</h1>
        <nav>
            <ul class="flex space-x-6">
                <li>
                    <a href="/" class="<%= currentPage === 'Home' ? 'bg-red-400' : 'hover:bg-red-400'%> py-2 px-4 rounded text-white font-medium transition duration-300">Home</a>
                </li>
                <li>
                    <a href="/host/add-home" class="<%= currentPage === 'addHome' ? 'bg-red-400' : 'hover:bg-red-400'%> py-2 px-4 rounded text-white font-medium transition duration-300">Add Home</a>
                </li>
            </ul>
        </nav>
    </header>